// Placeholder — real integration tests require a Firebase test environment.
// Run with: flutter test

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // TODO: add widget/integration tests once a Firebase emulator is wired up.
    expect(true, isTrue);
  });
}
